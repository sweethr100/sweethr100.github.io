from tkinter import *

window = Tk()

index = Label(window, text="0")
% = Button(window, text="%")
CE = Button(window,text="CE")
DEL = Button(window, text="DEL")
/ = Button(window, text="/")

window.mainloop()